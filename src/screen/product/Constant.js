import {ResponsiveUtil} from "../../util/ResponsiveUtil";

export const Constant = {
    WINDOW_WIDTH: ResponsiveUtil.screenWidth(),
    WINDOW_HEIGHT: ResponsiveUtil.screenHeight(),
    HEADER_IMAGE_HEIGHT: ResponsiveUtil.height(450)
}
